{
  "name": "quran-verses-demo",
  "version": "1.0.0",
  "description": "Small full-stack demo for a PM tech screen: Express backend + static frontend that fetches a random Qur’an verse from a local array.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "license": "MIT"
}
